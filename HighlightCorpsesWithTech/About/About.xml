<?xml version="1.0" encoding="utf-8"?>
<ModMetaData>
  <packageId>phildeluca.highlightcorpseswithtech</packageId>
  <name>Highlight Corpses With Tech</name>
  <author>Phil DeLuca</author>
  <supportedVersions>
    <li>1.5</li>
  </supportedVersions>
  <description>Marks corpses that still carry recoverable bionics and implants, so you can tell at a glance which bodies are worth hauling to a harvesting bench and which are already-stripped shells.

Qualifying corpses get a pulsing light-blue outline tracing the body, and an alert that names each corpse and what is on it. Clicking the alert cycles the camera through them. The outline brightens with the tech tier, so a rich corpse is distinguishable from a marginal one at a glance.

Which tech tiers count is configurable - one checkbox per tier, from peg legs up to archotech. The default is Industrial and up, so simple prosthetics count and peg legs do not.

Detection uses only vanilla API and no Harmony patches. It declares no dependency on any implant-recovery mod, though one is what makes this useful.</description>
  <!-- No modDependencies, and none is coming. This mod applies no Harmony patches:
       detection is vanilla API, the alert self-registers by subclassing
       RimWorld.Alert, and the outline is drawn by a MapComponent. Declaring a
       dependency it does not use would be noise - see hcwt-architecture.md
       section 5, and hcwt-test-plan.md T8.3 which guards it.

       Documents live in the umbrella folder prefixed hcwt-, never in this repo. -->
</ModMetaData>
